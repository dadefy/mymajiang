# 行尾由 git 定死，不依赖每台机器各自的 core.autocrlf。
#
# 为什么需要这条：这个仓库在多台 Windows 机器上克隆（core.autocrlf=true）。
# 不约束的话 `.sh` 会被**检出成 CRLF**，而这些脚本的本意是**传到 Linux 上由 bash 执行**
# （见 tools/ubuntu-deploy/）。CRLF 的脚本在 Linux 上会报
#   /usr/bin/env: 'bash\r': No such file or directory
# 或者更难懂的 $'\r': command not found —— 排查起来很费时间。
#
# 只约束「会被复制到别的机器上执行」的这几类；.ts / .mjs 之类不加限制，
# 免得给其它协作者制造无意义的换行改动。
#
# 改脚本后自检（必须为 0）：
#   tr -cd '\r' < 文件 | wc -c
*.sh      text eol=lf
*.service text eol=lf
*.conf    text eol=lf
# 整个 ubuntu-deploy 目录都按 LF 处理 —— 里面还有没扩展名的可执行脚本
# （mymj-tunnel-run，会被装到 /usr/local/bin 由 systemd 执行），
# 只按后缀匹配会漏掉它们，检出成 CRLF 后运行时才炸。
tools/ubuntu-deploy/** text eol=lf
