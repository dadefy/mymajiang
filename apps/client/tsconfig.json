{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // 客户端跑在浏览器/LayaAir 里：需要 DOM 类型，不使用 Node 的模块解析之外的任何 Node API。
    "lib": ["ES2022", "DOM"],
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"]
}
