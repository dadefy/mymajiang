node_modules/
dist/
coverage/
.env
.env.*
!.env.example

# 本地对象存储驱动落盘的文件（图片、语音）。属于运行时数据，不进版本库。
storage/

# 邀请密钥台账：里面是**明文密钥**，等同于账号凭据。绝对不要提交。
# 由 scripts/issue-keys.mjs 生成。
# 用通配是为了覆盖 `.keys-ledger.local.txt` 这类自建变体 ——
# 本地预览时用 KEYS_LEDGER 指过另一个文件名，那条规则单独的 .txt 挡不住。
.keys-ledger*.txt

# QA 本地隔离 pnpm store 与废弃目录
.qa-store/
_discarded-node_modules/

# 浏览器验收（tools/laya-presentation）的一次跑一份：截图 + 事件清单，属于验证产物而非工程资源。
# 必须带前导斜杠：只忽略仓库根下这一份，否则连 tools/laya-presentation/ 里的脚本本体也被吞掉。
/laya-presentation/
